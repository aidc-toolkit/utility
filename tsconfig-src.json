{
  "extends": "@aidc-toolkit/dev/tsconfig-template.json",
  "include": ["./src/**/*"],
  "compilerOptions": {
    // Modules.
    "rootDir": "src",

    // Emit.
    "outDir": "dist",

    // Projects.
    "composite": true
  }
}
